// script-global.js
document.addEventListener('DOMContentLoaded', () => {
    console.log('Página carregada com sucesso!');

    // Exemplo de como alterar idiomas
    const trocarIdioma = (idioma) => {
        alert(`Idioma alterado para: ${idioma}`);
    };
});
